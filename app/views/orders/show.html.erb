<div class="col-md-6 col-md-offset-3 text-center">
<table class="table table-striped table-responsive">
      <thead>
        <tr>
          <th>Quantité</th>
          <th>Désignation</th>
          <th>Nom du client</th>
        </tr>
      </thead>
      <tbody>
        <tr>

          <td class="success"><%= @order.order_meals.count %></td>
          <td class="success"><%= @order_meals.first.meal.name %></td>
          <td class="success"><%= @order.user.last_name %></td>
        </tr>
        <tr>
      </tbody>
    </table>
<%= link_to "Retour", user_path(current_user), class: "btn btn-success" %>
</div>

