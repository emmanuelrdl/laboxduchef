
<div class="container text-center">
  <div class="row">
    <div clas="col-xs-12 col-sm-6">
      <h4 class="title-body">CONFIRMATION DE VOTRE COMMANDE</h4>

      <% @order.order_meals.each do |order_meal| %>



      <div class="thumbnail paid-card col-xs-12 col-sm-6" >

        <h4><%= order_meal.meal.name %> </h4>

        <h5>A retirer le <%= order_meal.meal.starting_date %></h5>
        Le midi entre
        <p><%= order_meal.meal.take_away_noon_starts_at.strftime('%H:%M') %> et <%= order_meal.meal.take_away_noon_ends_at.strftime('%H:%M') %></p>
        En soirée entre
        <p><%= order_meal.meal.take_away_evening_starts_at.strftime('%H:%M') %> et <%= order_meal.meal.take_away_evening_ends_at.strftime('%H:%M') %></p>

        <p><%= order_meal.meal.restaurant.full_address %></p>
      </div>

      <% end %>
    <h6>Bon appetit et merci pour votre confiance</h6>
    <h5>Retour à "MON COMPTE" pour l'itinéraire</h5>
    </div>
  </div>



  <div style="margin: 10px"><%= link_to "Accueil", root_path, class: "btn btn-primary" %>
  <%= link_to "Mon compte", user_path(current_user), class: "btn btn-primary" %></div>

</div>
