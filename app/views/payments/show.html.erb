 <div id="body-container">
    <div class="container text-center">
      <div class="row">
        <div clas="col-xs-12 col-sm-6 col-sm-offset-3">
          <h4 class="title-body">CONFIRMATION DE VOTRE COMMANDE</h4>

          <% @order.order_meals.each do |order_meal| %>
            <div class="thumbnail col-xs-12 col-sm-6 col-sm-offset-3" >

              <h4><%= order_meal.meal.name %> x<%= order_meal.quantity %>   </h4>

              <p> <i class="fa fa-calendar-o"> </i> A retirer le <%= order_meal.meal.starting_date.strftime("%d/%m/%y") %> entre:</p>

              <div class="col-xs-12 ">
                <div class="col-xs-6">
                  <i class="fa fa-clock-o"></i> Midi
                  <p><%= order_meal.meal.take_away_noon_starts_at.strftime('%H:%M') %> - <%= order_meal.meal.take_away_noon_ends_at.strftime('%H:%M') %></p>
                </div>
                 <div class="col-xs-6">
                  <i class="fa fa-clock-o"></i> Soirée
                  <p><%= order_meal.meal.take_away_evening_starts_at.strftime('%H:%M') %> - <%= order_meal.meal.take_away_evening_ends_at.strftime('%H:%M') %></p>
                </div>
              </div>
              <i class="fa fa-map-marker" style="font-size:20px;"></i>
              <p style='margin-bottom:0px;'> <i> <%= order_meal.meal.restaurant.name %> </i> </p>
              <p><%= order_meal.meal.restaurant.full_address %></p>
             <h6> <i class="fa fa-exclamation-circle"></i> Pour des raisons sanitaires les plats sont à consommer le jour de l'achat </h6>
          </div>

          <% end %>
          <div class="col-xs-12">
            <p>Bon appetit et merci pour votre confiance !</p>
            <h6>Cliquez "mon compte" pour voir l'itinéraire</h6>
          </div>

        </div>
      </div>


      <div style="margin: 10px"><%= link_to "Accueil", root_path, class: "btn btn-primary" %>
      <%= link_to "Mon compte", user_path(current_user), class: "btn btn-primary" %></div>

    </div>

    <div style="height:80px;"></div>

</div>
